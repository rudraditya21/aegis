[package]
name = "aegis-core"
version = "0.1.0"
edition = "2024"

[dependencies]
packet-parser = { path = "../packet-parser" }
aho-corasick = "1.1"
chrono = { version = "0.4", default-features = false, features = ["clock"] }

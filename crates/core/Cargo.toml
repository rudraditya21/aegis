[package]
name = "aegis-core"
version = "0.1.0"
edition = "2024"

[dependencies]
config = { package = "aegis-config", path = "../config" }
packet-parser = { path = "../packet-parser" }
aho-corasick = "1.1"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
